window.ATTENDANCE_CONFIG = {
  // Paste the deployed Google Apps Script /exec URL below.
  webAppUrl: "",
  // Change this to a private random phrase, then use the SAME value in Code.gs.
  sharedKey: "CHANGE-ME-TO-A-PRIVATE-KEY",
  // Give each check-in phone a useful name, e.g. "Lawrence", "Front Door 1".
  deviceName: "Check-in Phone"
};
